#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../import-local/fixtures/cli.js" "$@"
else
  exec node  "$basedir/../../../import-local/fixtures/cli.js" "$@"
fi
# cmd-shim-target=C:/Users/WINDOWS/Documents/RepartoParlamentario/backend/node_modules/.pnpm/jest@30.3.0_@types+node@25.6.0/node_modules/import-local/fixtures/cli.js
